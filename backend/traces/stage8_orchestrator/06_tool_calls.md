# Tool Calls

1.  `view_file` to read `module_orchestrator.md` and understand pipeline structure.
2.  `view_file` to read `module_llm_client.md` to extract DeepSeek logic.
3.  `view_file` to read `api_reference.md` and `stream_thinking.md` to structure FastAPI endpoints and SSE logic.
4.  `write_to_file` to create `agents/llm_client.py`.
5.  `write_to_file` to create `orchestrator.py`.
6.  `run_command` to execute `mkdir api`.
7.  `write_to_file` to create `api/main.py` and `api/__init__.py`.
8.  `write_to_file` to output `postman_collection.json`.
9.  `write_to_file` commands to build the traces in `traces/stage8_orchestrator/`.
